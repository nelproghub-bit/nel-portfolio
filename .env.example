# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=nel_portfolio
DB_USER=root
DB_PASS=

# Application Configuration
ENVIRONMENT=production
BASE_PATH=/nel-portfolio

# On Vercel, use environment variables from Vercel dashboard
# For local development, copy this file to .env and update with your local values
